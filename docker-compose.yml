services:
  webdav:
    image: bytemark/webdav:latest
    container_name: bookmarksync
    restart: unless-stopped
    ports:
      - "8085:80"
    environment:
      AUTH_TYPE: Basic
      USERNAME: your_username_here
      PASSWORD: your_password_here
    volumes:
      - ./data:/var/lib/dav
